<% if @user.errors.any? %>
  <%= @user.errors.count %>error prohibited this book from being saved:
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

   <div class="container">
      <div class="row">
        <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6">
          <h2>User info</h2>
            <%= form_with model: @user do |f| %>
              <label for="inputName">Name</label></br>
              <%= f.text_field :name, autofocus: true, placeholder:"名前"%></br>

              <label for="inputImage">Image</label></br>
              <%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %></br>

              <label for="inputIntroduction">Introducion</label></br>
              <%= f.text_area :introduction, placeholder: "Introducion" %></br>

              <%= f.submit "Update User",class: "btn btn-secondary" %><br>

            <% end %>
        </div>
      </div>
    </div>
